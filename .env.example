# En desarrollo local no necesitas definir nada — el proxy de Vite redirige /api a localhost:8000
# En producción, configura esta variable con la URL del backend:
# VITE_API_BASE_URL=https://backend-gacha.vercel.app/api
